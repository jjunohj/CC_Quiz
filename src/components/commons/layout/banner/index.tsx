import styled from "@emotion/styled";

const Wrapper = styled.div`
  width: 100%;
  height: 600px;
  background-color: pink;
`;

export default function LayoutBanner() {
  return <Wrapper>배너영역</Wrapper>;
}
